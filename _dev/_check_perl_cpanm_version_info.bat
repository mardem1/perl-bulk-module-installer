
rem ==========================================================================
cmd.exe /c perl -v 
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c perl -V
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c cpanm --version
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c cpanm --help
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c cpanm  --no-interactive --info Perl::Critic
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c cpanm --no-interactive --scandeps Perl::Critic
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c cpanm --no-interactive --showdeps Perl::Critic
echo %ErrorLevel%
rem ==========================================================================

rem ==========================================================================
cmd.exe /c cpanm --no-interactive --verbose Perl::Critic
echo %ErrorLevel%
rem ==========================================================================
